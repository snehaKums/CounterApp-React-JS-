About

•	It gives simple UI how shopping cart works. 
•	There are multiple counters from which we can perform increment and decrement operations.
•	We can reset the counter value to 0 using reset button. Counter can be deleted by clicking Delete Button.
•	On top navBar total count on Counter is which works like Shopping cart .

How to run it?

Do npm start from command prompt
